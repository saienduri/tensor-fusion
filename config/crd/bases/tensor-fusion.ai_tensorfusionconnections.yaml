---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: tensorfusionconnections.tensor-fusion.ai
spec:
  group: tensor-fusion.ai
  names:
    kind: TensorFusionConnection
    listKind: TensorFusionConnectionList
    plural: tensorfusionconnections
    singular: tensorfusionconnection
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.connectionURL
      name: Connection URL
      type: string
    - jsonPath: .status.workerName
      name: Worker Name
      type: string
    - jsonPath: .spec.workloadName
      name: Workload Name
      type: string
    - jsonPath: .spec.clientPod
      name: Client Pod
      priority: 1
      type: string
    - jsonPath: .spec.externalClient
      name: External
      priority: 1
      type: boolean
    - jsonPath: .status.expiresAt
      name: Expires At
      priority: 1
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: TensorFusionConnection is the Schema for the tensorfusionconnections
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TensorFusionConnectionSpec defines the desired state of TensorFusionConnection.
            properties:
              clientPod:
                description: ClientPod is the name of the client pod (for in-cluster
                  clients)
                type: string
              externalClient:
                description: |-
                  ExternalClient configuration for clients outside the Kubernetes cluster
                  Mutually exclusive with ClientPod
                properties:
                  clientId:
                    description: Unique identifier for the external client
                    type: string
                  ttlSeconds:
                    description: TTL in seconds - connection will be garbage collected
                      after this duration
                    format: int64
                    maximum: 86400
                    minimum: 60
                    type: integer
                required:
                - ttlSeconds
                type: object
              workloadName:
                type: string
            required:
            - workloadName
            type: object
          status:
            description: TensorFusionConnectionStatus defines the observed state of
              TensorFusionConnection.
            properties:
              connectionURL:
                type: string
              expiresAt:
                description: ExpiresAt is when this connection expires (for external
                  clients with TTL)
                format: date-time
                type: string
              phase:
                type: string
              workerName:
                type: string
            required:
            - connectionURL
            - phase
            - workerName
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
